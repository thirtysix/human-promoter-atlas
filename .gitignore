# Built data artifacts (large; rebuild with data/build_app_db.py)
data/canonical_promoter.duckdb
data/aggregate/
data/gtex/
data/depmap/
data/network/

# Python
__pycache__/
*.pyc
*.pyo
*.egg-info/
.venv/
venv/
.pytest_cache/

# Streamlit
.streamlit/secrets.toml

# Docker
.docker-build-cache/

# Build info — written by deploy step, baked into the image, never committed
app/_build_info.txt

# UX-audit screenshots (artifacts — regenerate with tools/screenshot.py)
docs/ux-audit/

# Env
.env
.env.local

# Editor
.idea/
.vscode/
*.swp

# OS
.DS_Store
Thumbs.db

# Auto-generated project context for Claude Code
CLAUDE.md

# Working notes — kept locally, deliberately untracked (see commit-hygiene rule:
# nothing outside README.md is committed without asking, and handoff notes tend
# to accumulate whatever was on the desk — private repo names, host paths, the
# state of unpushed work).
nextsession.md
LessonsLearned.md
